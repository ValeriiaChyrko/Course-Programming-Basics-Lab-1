namespace Calculator;

public static class OperationsPlus
{
    public static int Plus(int x, int y)
    {
        // TODO #1. Uncomment the line below and remove the line that throws the NotImplementedException.
        return x + y;
    }

    public static int PlusThree(int x, int y, int z)
    {
        // TODO #2. Write a method to calculate the sum of three numbers.
        return x + y + z;
    }

    public static int PlusDouble(int x)
    {
        // TODO #3. Write a method that adds a given number to its double.
        return x + (x * x);
    }
}